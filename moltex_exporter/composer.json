{
    "name": "moltex/exporter",
    "description": "Development dependencies for the Moltex Exporter WordPress plugin.",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.4",
        "ext-json": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "scripts": {
        "test": "phpunit tests"
    },
    "config": {
        "allow-plugins": {},
        "sort-packages": true
    }
}
